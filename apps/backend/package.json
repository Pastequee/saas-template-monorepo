{
  "name": "@repo/backend",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "imports": {
    "#routers/*": "./src/routers/*.ts",
    "#lib/*": "./src/lib/*.ts",
    "#middlewares/*": "./src/middlewares/*.ts",
    "#services/*": "./src/services/*.ts"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build --compile --sourcemap --minify src/index.ts --outfile dist/server",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.1",
    "@elysiajs/openapi": "^1.4.13",
    "@repo/auth": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/env": "workspace:*",
    "@repo/utils": "workspace:*",
    "drizzle-orm": "catalog:",
    "elysia": "catalog:",
    "pino": "^10.1.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/pg": "catalog:",
    "picocolors": "^1.1.1",
    "pino-pretty": "^13.1.3"
  }
}
