{
  "name": "@repo/root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev:elysia": "turbo run -F @repo/web-elysia dev",
    "dev:convex": "turbo run -F @repo/web-convex dev",
    "lint": "ultracite check",
    "format": "ultracite fix --unsafe",
    "typecheck": "turbo run typecheck",
    "db:push": "pnpm -F @repo/db push",
    "db:migrate": "pnpm -F @repo/db migrate",
    "db:generate": "pnpm -F @repo/db generate",
    "db:studio": "pnpm -F @repo/db studio",
    "auth:generate": "pnpm -F @repo/auth generate"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "@repo/tsconfig": "workspace:*",
    "@types/node": "^24.9.2",
    "lefthook": "^2.0.1",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3",
    "ultracite": "^6.0.5"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=24"
  }
}
