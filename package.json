{
  "name": "@repo/root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "ultracite check",
    "format": "ultracite fix --unsafe",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build:web": "bun -F ./apps/web build",
    "build:backend": "bun -F ./apps/backend build",
    "build:docker:web": "docker build -f apps/web/Dockerfile -t web-image .",
    "build:docker:backend": "docker build -f apps/backend/Dockerfile -t backend-image .",
    "db:push": "bun -F ./packages/db push",
    "db:migrate": "bun -F ./packages/db migrate",
    "db:gen": "bun -F ./packages/db generate",
    "db:studio": "bun -F ./packages/db studio",
    "auth:gen": "bun -F ./packages/auth generate",
    "docker:up": "docker compose -f infra/docker/docker-compose.dev.yml up -d",
    "docker:down": "docker compose -f infra/docker/docker-compose.dev.yml down -v",
    "docker:reset": "bun docker:down && bun docker:up"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@repo/tsconfig": "workspace:*",
    "@types/node": "^24.10.4",
    "@typescript/native-preview": "^7.0.0-dev.20251214.1",
    "lefthook": "^2.0.11",
    "turbo": "^2.6.3",
    "typescript": "^5.9.3",
    "ultracite": "^6.4.0"
  },
  "packageManager": "bun@1.3.4",
  "engines": {
    "node": ">=24"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "tooling/*"
    ],
    "catalog": {
      "@t3-oss/env-core": "^0.13.8",
      "drizzle-orm": "^1.0.0-beta.2-f9236e3",
      "@types/pg": "^8.16.0",
      "better-auth": "^1.4.7",
      "elysia": "^1.4.19",
      "zod": "^4.1.13",
      "@types/bun": "^1.3.4"
    }
  }
}