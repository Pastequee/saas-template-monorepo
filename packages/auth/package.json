{
  "name": "@repo/auth",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "bun dotenv bunx @better-auth/cli generate --config ./src/index.ts --output ../db/prisma/models/auth.prisma",
    "typecheck": "tsgo --noEmit",
    "dotenv": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@t3-oss/env-core": "catalog:",
    "better-auth": "catalog:",
    "dotenv-cli": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*"
  }
}
