{
  "name": "@repo/auth",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "pnpm dotenv pnpm dlx @better-auth/cli generate --config ./src/index.ts --output ../db/src/schemas/auth.ts",
    "typecheck": "tsgo --noEmit",
    "dotenv": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@t3-oss/env-core": "catalog:",
    "better-auth": "catalog:",
    "dotenv-cli": "catalog:",
    "zod": "catalog:"
  }
}
