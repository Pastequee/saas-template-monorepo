{
  "name": "@repo/db",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts"
  },
  "scripts": {
    "migrate": "pnpm dotenv drizzle-kit migrate dev",
    "generate": "pnpm dotenv drizzle-kit generate",
    "push": "pnpm dotenv drizzle-kit push",
    "studio": "pnpm dotenv drizzle-kit studio",
    "typecheck": "tsgo --noEmit",
    "dotenv": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@t3-oss/env-core": "catalog:",
    "dotenv-cli": "catalog:",
    "drizzle-orm": "catalog:",
    "pg": "^8.16.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/pg": "catalog:",
    "drizzle-kit": "^0.31.6"
  }
}
