{
  "name": "@repo/db",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts"
  },
  "scripts": {
    "migrate": "bun dotenv drizzle-kit migrate dev",
    "generate": "bun dotenv drizzle-kit generate",
    "push": "bun dotenv drizzle-kit push",
    "studio": "bun dotenv drizzle-kit studio",
    "typecheck": "tsc --noEmit",
    "dotenv": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "dotenv-cli": "catalog:",
    "drizzle-orm": "catalog:",
    "@t3-oss/env-core": "catalog:",
    "zod": "catalog:",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "catalog:",
    "drizzle-kit": "^0.31.5"
  }
}
