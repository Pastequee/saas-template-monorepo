{
  "name": "@repo/db",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "deploy": "bun dotenv prisma migrate deploy",
    "migrate": "bun dotenv prisma migrate dev",
    "generate": "bun dotenv prisma generate",
    "push": "bun dotenv prisma db push",
    "studio": "bun dotenv prisma studio",
    "typecheck": "tsc --noEmit",
    "dotenv": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^6.17.0",
    "@prisma/client": "catalog:",
    "dotenv-cli": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "prisma": "catalog:",
    "prisma-zod-generator": "^1.27.3"
  }
}
